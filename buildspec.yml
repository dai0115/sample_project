version: 0.2

phases:
  install:
    runtime-versions:
      docker: 20
  pre_build:
    commands:
      - echo pre_build phase
  build:
    commands:
      - echo build phase
  post_build:
    commands:
      - echo post_build phase
      - pip install boto3
